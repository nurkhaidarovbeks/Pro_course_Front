# Lab 5.1 - Typed Components & Props

This project demonstrates TypeScript interfaces and type aliases for props validation.
UserCard used typed props with optional values and React.ReactNode children.
SkillList uses a union type for conditional rendering.