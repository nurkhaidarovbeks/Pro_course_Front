Code splitting allows applications to load only the code needed for the current page.

React.lazy() loads components dynamically using import().

Suspense displays fallback UI while lazy components are loading.

Error Boundaries catch errors in components and show fallback UI instead of crashing the app.