# Lab 3.1 - Step Counter

## How to run
npm install 
npm run dev

## Description
This lab demonstrates React props and state using useState.
Each StepCounter receives initial values via props and manages its own internal state independently.